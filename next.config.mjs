/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /* Hide the bottom-left dev indicator so it doesn't overlap the
     in-app "..." menu button (which sits in the same screen corner). */
  devIndicators: false,
};

export default nextConfig;
